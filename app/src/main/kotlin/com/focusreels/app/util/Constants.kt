package com.focusreels.app.util

/** Identifiants d'applications gérées. V1 : Instagram uniquement (cahier des charges §3.5, architecture modulaire). */
object AppIds {
    const val INSTAGRAM = "com.instagram.android"
}

object Defaults {
    const val BASE_DELAY_SECONDS = 5
    const val INCREMENT_SECONDS = 5
    const val RELOCK_DELAY_MINUTES = 30
    const val TOLERATED_SWIPES_AFTER_DM = 1
}
